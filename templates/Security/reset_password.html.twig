{% extends '@DdrBridge/base.html.twig' %}

{% block title %}Passwort zurücksetzen{% endblock %}

{% block ddr_main_container_content %}
    <h1 class="ddr-page-header">{{ 'reset_password.title'|trans({}, 'ddr_security')}}</h1>

    {% if mailSent %}
        <div class="alert alert-info">
            Sofern die Email Adresse unserem System bekannt war, wurde eine Email mit Anweisungen zum Zurücksetzen des Passworts verschickt.
        </div>
    {% else %}
        {% if user is not null %}
            <div class="alert alert-info">Das Passwort muss mindestens 8 Zeichen lang sein.</div>
            <div class="alert alert-info">Das Passwort muss mindestens drei der folgenden Anforderungen erfüllen: Mindestens ein Kleinbuchstabe, Mindestens ein
                Großbuchstabe, Mindestens eine Zahl, Mindestens ein Sonderzeichen (ohne Währungssymbole).</div>
            {{ form(resetForm) }}
        {% else %}
            {{ form(requestForm) }}
        {% endif %}

    {% endif %}
{% endblock ddr_main_container_content %}
