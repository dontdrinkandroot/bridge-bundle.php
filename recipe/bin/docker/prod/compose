#!/bin/bash

if [ -f "docker/prod/docker-compose.override.yaml" ]; then
    docker compose -f docker/prod/docker-compose.yaml -f docker/prod/docker-compose.override.yaml "$@"
else
    docker compose -f docker/prod/docker-compose.yaml "$@"
fi
